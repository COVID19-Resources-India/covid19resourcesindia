# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
  # Directory that contains the deploy-ready HTML files and assets generated by
  # the build. This is relative to the base directory if one has been set, or the
  # root directory if a base has not been set. This sample publishes the
  # directory located at the absolute path "root/project/build-output"
  publish = "build/"

  # Default build command.
  command = "yarn build"

# Production context: all deploys from the Production branch set in your site’s
# deploy contexts will inherit these settings.
[context.production]
  publish = "build/"
  command = "yarn build"
[context.production.environment]
  FIREBASE_API_KEY = "AIzaSyAGU9ZzeCnVvVKmhRItqIdGS6a2kyHApfA"
  FIREBASE_APP_ID = "1:267056756471:web:ddf6afab7f1761cf7bc5f2"
  FIREBASE_MESSAGING_SENDER_ID = "267056756471"
  FIREBASE_MEASUREMENT_ID = "G-DT9BR5XTJE"
  REACT_APP_SECRET_ENCRYPT_KEY = "MrMYAK53SOW6hF4AtdWPrkFelZsJRu"
  NODE_ENV = "production"

# Branch Deploy context: all deploys that are not from a pull/merge request or
# from the Production branch will inherit these settings.
[context.branch-deploy]
  command = "yarn build"
[context.branch-deploy.environment]
  FIREBASE_API_KEY = "AIzaSyAGU9ZzeCnVvVKmhRItqIdGS6a2kyHApfA"
  FIREBASE_APP_ID = "1:267056756471:web:ddf6afab7f1761cf7bc5f2"
  FIREBASE_MESSAGING_SENDER_ID = "267056756471"
  FIREBASE_MEASUREMENT_ID = "G-DT9BR5XTJE"
  REACT_APP_SECRET_ENCRYPT_KEY = "MrMYAK53SOW6hF4AtdWPrkFelZsJRu"
  NODE_ENV = "development"

# Redirects and headers are GLOBAL for all builds – they do not get scoped to
# contexts no matter where you define them in the file.
# For context-specific rules, use _headers or _redirects files, which are
# PER-DEPLOY.

# A basic redirect rule
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
